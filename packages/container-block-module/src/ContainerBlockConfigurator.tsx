import React from 'react'
import { BlockConfigurator } from '@compoz/core'
import { ContainerBlock } from './index'

const ContainerBlockConfigurator: BlockConfigurator<ContainerBlock> = ({ block }) => {
    return null
}

export default ContainerBlockConfigurator
